name: Debug CI Runner

on:
  workflow_dispatch:
  push:
    branches:
      - claude/sync-lovable-production-y7aiaw

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello from a runner"
